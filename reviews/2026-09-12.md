# 复盘 — lootlore(总站)— 2026-09-12(夜,09-11 23:30 北京时起)

> 本轮性质:**总站自有门户页 SEO 补齐 + 五子站快照同步轮**。

## 一、本轮动作(已做,Actions gates 全绿)

1. ✅ commit `ee4a152` **seo: hub portal pages JSON-LD/og/canonical, image dims, portal interlinks**:
   - 做了什么:总站自有的 11 个门户页(首页/about/contact/guides/reviews/updates/terms/privacy-policy/disclaimer/editorial-policy/404,tech-audit 修前数记为这 11 页"一个 JSON-LD 都没有")补齐 JSON-LD——首页 WebSite+Organization+BreadcrumbList,about/contact 用 AboutPage/ContactPage,guides/reviews/updates 用 CollectionPage,其余 WebPage,均带 BreadcrumbList;同批补 og:description、og:image、twitter:card;首页 5 张展示图补 width/height(tech-audit 修前数记为"5/511 缺尺寸,全部在首页");首页新增两处正文内链指向 /guides 与 /updates,terms 与 privacy-policy 互加一句正文链接,修掉其中 4 个 0 入链页(tech-audit 修前数记 41 页入链≤1,含总站自有的 guides/privacy-policy/terms/updates 等)。`build.py` 新增 `hub_jsonld()`/`og_extra_snippets()`,`config/hub.json` 各卡片加 `img_w`/`img_h`。
   - 为什么:tech-audit 修前数把"总站门户页无 JSON-LD/og 大面积缺/首页展示图缺尺寸/门户页互不导流"列为该站最值得修的前三项。
   - 验证结果:diff 24 个文件;Actions gates 绿。
   - 遗留:`/sephiria/author/` 等继承自子站的孤儿页未在本次处理范围(属子站源头问题)。

2. ✅ commit `67d4878` **sync: sources from subsites 2026-09-12 (post JSON-LD/hreflang/trust pages)**:
   - 做了什么:重新快照五子站(sephiria/dragonsword/orc 的 Article+BreadcrumbList JSON-LD 与 frontmatter date 回填;Beast 的 Article+BreadcrumbList JSON-LD、hreflang 重建、en-only author 页;shift 的 Article+BreadcrumbList JSON-LD 与 3 个新 trust 页)并重建总站;`build.py` 的绝对 URL 重写规则(canonical/og/hreflang/JSON-LD 统一改写到总站域名 + `/<slug>` 前缀)保持不变。
   - 为什么:五子站当晚各自做了 SEO 修复,总站需要跟着重新聚合快照。
   - 验证结果:diff 923 个文件;commit message 自述用 `json.loads` + `tech-audit.py` 验证 0 处 JSON-LD 解析错误、0 处 hreflang 互指不匹配;gates(`check_content`/`check_i18n`/`check_sitemap`/`link_check`)全部本地通过,365 个 html、0 死链。Actions gates 绿。
   - 遗留:hreflang 15 页互指不完整的问题继承自 Beast 源站,build.py 原样复制未破坏,需从 Beast 源头修。

## 二、行动项(下轮)

- [ ] `/sephiria/author/` 等继承自子站的孤儿页入链问题,待子站源头处理后随下次同步自动解决
- [ ] 全站 0 处 GA4/Clarity 埋点(09-11 已记录,`hub.json` 的 `ga4_id` 仍为空)
- [ ] 数据轮五个数
